// SPECIMEN (frame-v2#4 · module_chain main.rs branch): items in `src/main.rs` also live in the
// CRATE ROOT → module chain is `[]`. Guards adapter.rs:123 (the main special-case, distinct from lib).
pub struct MainRootItem {
    field: u8,
}
